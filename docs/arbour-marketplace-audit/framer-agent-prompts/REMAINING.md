# Remaining prompts — credit-tight (20:42 verify)

Live: https://arbour.framer.website · `Last-Modified` **20:30:42 GMT** · score **~8.8**. Still not Featured.

**Cerrado (no re-correr):** scrim Home (`Hero Bottom Gradient` + `Meta Contrast Scrim` + `Vignette`), `header` marketing, 404 enlazado, `lang=en`, 4 alts, Privacy, Neighbourhoods, full-bleed.

Si Notes 05 / hover / `[ ]` se ven en el **canvas** y no aquí: **Publish** el preview. Este archivo mide solo `arbour.framer.website`.

## Cómo no quemar créditos

1. **New Chat** cada bloque. Fast Mode **Off**. Reasoning **Light**.
2. Pega **solo** el bloque entre `---`. **No** pegues `00-constraints.md`.
3. `@` + layer **antes** de enviar. Para. **No** `/code`. **No** Fable / Sol.

---

## 01 — Número 05 + bind Home

**Picker:** GPT 5.6 Luna · Light · `/cms` · Fast Off  
**@** collection **Notes** + lista journal de `Home`  
**Esfuerzo:** 0.4×

Live `/notes` featured “Reading a façade as a ledger of ownership.” no muestra índice. Live Home waiting sigue `[ ]`.

---

/cms
1. Notes item "Reading a façade as a ledger of ownership." — set its number/index field to 05. It currently has none on the published page.
2. Home journal card "The case for waiting." still shows "[ ]" on https://arbour.framer.website. Bind that number to the same Notes number field used on /notes. That item is 04.
Do not add, delete, unpublish, or rename notes. Stop.

---

## 02 — Hover en 3 links (publicado)

**Picker:** Sonnet 5 · Light · `/component` · Fast Off  
**@** `Home` · selecciona `EXPLORE →`, `VIEW ALL →`, `VIEW ALL NOTES →`  
**Esfuerzo:** 0.6×

On the published site, EXPLORE already has a child span at opacity 0.35 with scaleX 0. `:hover` is true; that span does **not** go to scaleX 1. Wire Hover so that underline/opacity actually changes (opacity 0.55 or the existing span scaleX 1, 180ms). Pressed opacity 0.4. If they share one component, edit it once. Do not change color or type. Stop.

---

## Fuera del Agent

- **Favicon:** súbelo tú (`default-favicon-light.v1.png`).
- **Publish** después de 01–02 o el preview no cambia.
- Lighthouse: humano.

**Total Agent si Luna está:** ~1.0× un GPT 5.5.
