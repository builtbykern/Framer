# Halden Photographer

Live Framer Marketplace template ($29). Photography portfolio: Home is a pannable Drift Plane; each CMS series opens its own split project page. Canvas lives in Framer; this folder is the local source preview of the same structure.

```bash
cd templates/halden
npm install
npm run dev
```

Spec: [SPEC.md](SPEC.md)
