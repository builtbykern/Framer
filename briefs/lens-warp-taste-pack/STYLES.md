# Lens Warp — styles / tokens

Component is mostly image + shader. Minimal chrome.

## Color
- Default edge fill / paper: `#F4F3F0`
- Ink (if any label): `#0A0A0A`
- No brand accent required for v1

## Type
Only if showing a tiny caption in preview frame (outside component): one grotesk 12–14px.

## Layout
- Component fills parent width by default
- No DialKit docks, no preset chips inside the component
