const fs = require("fs")
const src = fs.readFileSync("/Users/noel/Desktop/Framer/.tmp/halden-src/Series_Stills.tsx", "utf8")
const info = await framer.getProjectInfo()
if (info.name !== "Halden") throw new Error(`Wrong project: ${info.name}`)
const stills = (await framer.getCodeFiles()).find((f) => f.id === "jeA2cvO")
if (!stills) throw new Error("missing Series_Stills")
console.log(JSON.stringify({ project: info.name, bytes: src.length }, null, 2))
await stills.setFileContent(src)
console.log("wrote")
