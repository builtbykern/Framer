# Restage Archive Preview Home Desktop with Kern atmosphere + champagne accent

**Status:** executed 2026-08-09 — also `/thumbnail` via `archivepreview-thumbnail.mjs`; component bg `transparent`.

## Outcome

Home + `/thumbnail` use BuiltByKern atmosphere (`#060606` + liquid + champagne blooms). Component background is transparent so the page shows through.

## Scripts

- `node scripts/framer/archivepreview-home.mjs`
- `node scripts/framer/archivepreview-thumbnail.mjs`
