const content="import * as React from \"react\"\nimport { addPropertyControls, ControlType, useIsStaticRenderer } from \"framer\"\n\ninterface EdgeRefractionShaderProps {\n    topEnabled: boolean\n    bottomEnabled: boolean\n    edgeDepth: number\n    curvature: number\n    stretch: number\n    scrollResponse: number\n    idleAmount: number\n    idleSpeed: number\n    style?: React.CSSProperties\n}\n\ntype RuntimeParams = {\n    topEnabled: boolean\n    bottomEnabled: boolean\n    edgeDepth: number\n    curvature: number\n    stretch: number\n    scrollResponse: number\n    idleAmount: number\n    idleSpeed: number\n}\n\ntype ContentWarp = {\n    pageRoot: HTMLElement\n    overlayHost: HTMLElement\n    svg: SVGSVGElement\n    filter: SVGFilterElement\n    filterId: string\n    edgeMask: SVGFEImageElement\n    yMap: SVGFEImageElement\n    xMap: SVGFEImageElement\n    dispY: SVGFEDisplacementMapElement\n    dispX: SVGFEDisplacementMapElement\n    targets: HTMLElement[]\n    prevFilter: Map<HTMLElement, string>\n    lastW: number\n    lastH: number\n    lastMaskKey: string\n}\n\nconst QUIET_MS = 180\nconst QUIET_INTENSITY = 0.02\n\nfunction resolvePageRoot(start: HTMLElement | null): HTMLElement | null {\n    if (typeof document === \"undefined\" || !start) return null\n    let el: HTMLElement | null = start.parentElement\n    let best: HTMLElement | null = el\n    while (el && el !== document.body && el !== document.documentElement) {\n        const name = (el.getAttribute(\"data-framer-name\") || \"\").toLowerCase()\n        if (\n            name === \"desktop\" ||\n            name === \"tablet\" ||\n            name === \"phone\" ||\n            name === \"primary\" ||\n            name === \"breakpoint\"\n        ) {\n            return el\n        }\n        if (el.childElementCount >= 2) best = el\n        const viewH =\n            typeof window !== \"undefined\" ? window.innerHeight || 800 : 800\n        if (el.childElementCount >= 2 && (el.scrollHeight || 0) > viewH * 1.2) {\n            best = el\n        }\n        el = el.parentElement\n    }\n    return best\n}\n\nfunction buildYMap(width: number, height: number): string {\n    const w = Math.max(1, Math.floor(width))\n    const h = Math.max(1, Math.floor(height))\n    const svg = `<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"${w}\" height=\"${h}\" viewBox=\"0 0 ${w} ${h}\" preserveAspectRatio=\"none\"><defs><linearGradient id=\"g\" x1=\"0\" y1=\"0\" x2=\"0\" y2=\"1\"><stop offset=\"0%\" stop-color=\"rgb(255,255,255)\"/><stop offset=\"50%\" stop-color=\"rgb(128,128,128)\"/><stop offset=\"100%\" stop-color=\"rgb(0,0,0)\"/></linearGradient></defs><rect width=\"${w}\" height=\"${h}\" fill=\"url(#g)\"/></svg>`\n    return `data:image/svg+xml;utf8,${encodeURIComponent(svg)}`\n}\n\nfunction buildXMap(width: number, height: number): string {\n    const w = Math.max(1, Math.floor(width))\n    const h = Math.max(1, Math.floor(height))\n    const svg = `<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"${w}\" height=\"${h}\" viewBox=\"0 0 ${w} ${h}\" preserveAspectRatio=\"none\"><defs><linearGradient id=\"g\" x1=\"0\" y1=\"0\" x2=\"1\" y2=\"0\"><stop offset=\"0%\" stop-color=\"rgb(40,40,40)\"/><stop offset=\"50%\" stop-color=\"rgb(128,128,128)\"/><stop offset=\"100%\" stop-color=\"rgb(220,220,220)\"/></linearGradient></defs><rect width=\"${w}\" height=\"${h}\" fill=\"url(#g)\"/></svg>`\n    return `data:image/svg+xml;utf8,${encodeURIComponent(svg)}`\n}\n\n/** White bands only at current viewport top/bottom inside the page root. */\nfunction buildViewportEdgeMask(options: {\n    width: number\n    height: number\n    yInRoot: number\n    viewH: number\n    edgeFrac: number\n    topEnabled: boolean\n    bottomEnabled: boolean\n}): string {\n    const w = Math.max(1, Math.floor(options.width))\n    const h = Math.max(1, Math.floor(options.height))\n    const viewH = Math.max(1, options.viewH)\n    const band = Math.max(\n        28,\n        viewH * Math.max(0.1, Math.min(0.4, options.edgeFrac))\n    )\n    const y0 = Math.max(0, options.yInRoot)\n    const y1 = Math.min(h, options.yInRoot + viewH)\n    const topRect = options.topEnabled\n        ? `<rect x=\"0\" y=\"${y0}\" width=\"${w}\" height=\"${band}\" fill=\"white\"/>`\n        : \"\"\n    const bottomRect = options.bottomEnabled\n        ? `<rect x=\"0\" y=\"${Math.max(y0, y1 - band)}\" width=\"${w}\" height=\"${band}\" fill=\"white\"/>`\n        : \"\"\n    const svg = `<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"${w}\" height=\"${h}\" viewBox=\"0 0 ${w} ${h}\" preserveAspectRatio=\"none\"><rect width=\"${w}\" height=\"${h}\" fill=\"black\"/>${topRect}${bottomRect}</svg>`\n    return `data:image/svg+xml;utf8,${encodeURIComponent(svg)}`\n}\n\nfunction setHref(el: SVGFEImageElement, href: string) {\n    el.setAttribute(\"href\", href)\n    el.setAttributeNS(\"http://www.w3.org/1999/xlink\", \"href\", href)\n}\n\nfunction createContentWarp(\n    pageRoot: HTMLElement,\n    overlayHost: HTMLElement,\n    topEnabled: boolean,\n    bottomEnabled: boolean\n): ContentWarp | null {\n    if (typeof document === \"undefined\") return null\n    const ns = \"http://www.w3.org/2000/svg\"\n    const filterId = `ers-${Math.random().toString(36).slice(2, 10)}`\n\n    const svg = document.createElementNS(ns, \"svg\")\n    svg.setAttribute(\"width\", \"0\")\n    svg.setAttribute(\"height\", \"0\")\n    svg.setAttribute(\"aria-hidden\", \"true\")\n    svg.style.position = \"absolute\"\n    svg.style.width = \"0\"\n    svg.style.height = \"0\"\n    svg.style.overflow = \"hidden\"\n    svg.style.pointerEvents = \"none\"\n\n    const defs = document.createElementNS(ns, \"defs\")\n    const filter = document.createElementNS(ns, \"filter\")\n    filter.setAttribute(\"id\", filterId)\n    filter.setAttribute(\"filterUnits\", \"userSpaceOnUse\")\n    filter.setAttribute(\"primitiveUnits\", \"userSpaceOnUse\")\n    filter.setAttribute(\"x\", \"0\")\n    filter.setAttribute(\"y\", \"0\")\n    filter.setAttribute(\"width\", \"100\")\n    filter.setAttribute(\"height\", \"100\")\n    filter.setAttribute(\"color-interpolation-filters\", \"sRGB\")\n\n    const edgeMask = document.createElementNS(ns, \"feImage\")\n    edgeMask.setAttribute(\"result\", \"edgeMask\")\n    edgeMask.setAttribute(\"preserveAspectRatio\", \"none\")\n\n    const yMap = document.createElementNS(ns, \"feImage\")\n    yMap.setAttribute(\"result\", \"yMap\")\n    yMap.setAttribute(\"preserveAspectRatio\", \"none\")\n\n    const xMap = document.createElementNS(ns, \"feImage\")\n    xMap.setAttribute(\"result\", \"xMap\")\n    xMap.setAttribute(\"preserveAspectRatio\", \"none\")\n\n    const yEdge = document.createElementNS(ns, \"feComposite\")\n    yEdge.setAttribute(\"in\", \"yMap\")\n    yEdge.setAttribute(\"in2\", \"edgeMask\")\n    yEdge.setAttribute(\"operator\", \"arithmetic\")\n    yEdge.setAttribute(\"k1\", \"1\")\n    yEdge.setAttribute(\"k2\", \"0\")\n    yEdge.setAttribute(\"k3\", \"0\")\n    yEdge.setAttribute(\"k4\", \"0\")\n    yEdge.setAttribute(\"result\", \"yEdge\")\n\n    const xEdge = document.createElementNS(ns, \"feComposite\")\n    xEdge.setAttribute(\"in\", \"xMap\")\n    xEdge.setAttribute(\"in2\", \"edgeMask\")\n    xEdge.setAttribute(\"operator\", \"arithmetic\")\n    xEdge.setAttribute(\"k1\", \"1\")\n    xEdge.setAttribute(\"k2\", \"0\")\n    xEdge.setAttribute(\"k3\", \"0\")\n    xEdge.setAttribute(\"k4\", \"0\")\n    xEdge.setAttribute(\"result\", \"xEdge\")\n\n    const dispY = document.createElementNS(ns, \"feDisplacementMap\")\n    dispY.setAttribute(\"in\", \"SourceGraphic\")\n    dispY.setAttribute(\"in2\", \"yEdge\")\n    dispY.setAttribute(\"xChannelSelector\", \"R\")\n    dispY.setAttribute(\"yChannelSelector\", \"R\")\n    dispY.setAttribute(\"scale\", \"0\")\n    dispY.setAttribute(\"result\", \"warpY\")\n    dispY.setAttribute(\"data-ers\", \"y\")\n\n    const dispX = document.createElementNS(ns, \"feDisplacementMap\")\n    dispX.setAttribute(\"in\", \"warpY\")\n    dispX.setAttribute(\"in2\", \"xEdge\")\n    dispX.setAttribute(\"xChannelSelector\", \"R\")\n    dispX.setAttribute(\"yChannelSelector\", \"R\")\n    dispX.setAttribute(\"scale\", \"0\")\n    dispX.setAttribute(\"data-ers\", \"x\")\n\n    filter.appendChild(edgeMask)\n    filter.appendChild(yMap)\n    filter.appendChild(xMap)\n    filter.appendChild(yEdge)\n    filter.appendChild(xEdge)\n    filter.appendChild(dispY)\n    filter.appendChild(dispX)\n    defs.appendChild(filter)\n    svg.appendChild(defs)\n    // Keep SVG inside page root so url(#id) resolves on siblings\n    pageRoot.insertBefore(svg, pageRoot.firstChild)\n\n    const prevFilter = new Map<HTMLElement, string>()\n    const targets: HTMLElement[] = []\n    const children = Array.from(pageRoot.children) as HTMLElement[]\n    for (const child of children) {\n        if (child === svg) continue\n        if (child === overlayHost) continue\n        if (overlayHost.contains(child)) continue\n        if (child.contains(overlayHost)) continue\n        const tag = child.tagName.toLowerCase()\n        if (tag === \"script\" || tag === \"style\" || tag === \"svg\") continue\n        prevFilter.set(child, child.style.filter || \"\")\n        const url = `url(#${filterId})`\n        child.style.filter = url\n        ;(child.style as CSSStyleDeclaration & { WebkitFilter?: string }).WebkitFilter =\n            url\n        targets.push(child)\n    }\n\n    // Silence unused toggles at create time — mask rebuilt on first sync\n    void topEnabled\n    void bottomEnabled\n\n    return {\n        pageRoot,\n        overlayHost,\n        svg,\n        filter,\n        filterId,\n        edgeMask,\n        yMap,\n        xMap,\n        dispY,\n        dispX,\n        targets,\n        prevFilter,\n        lastW: 0,\n        lastH: 0,\n        lastMaskKey: \"\",\n    }\n}\n\nfunction destroyContentWarp(warp: ContentWarp) {\n    for (const el of warp.targets) {\n        const prev = warp.prevFilter.get(el) || \"\"\n        el.style.filter = prev\n        ;(el.style as CSSStyleDeclaration & { WebkitFilter?: string }).WebkitFilter =\n            prev\n    }\n    warp.svg.remove()\n}\n\nfunction syncContentWarp(\n    warp: ContentWarp,\n    runtime: RuntimeParams,\n    intensity: number,\n    reducedMotion: boolean\n) {\n    const rect = warp.pageRoot.getBoundingClientRect()\n    const contentW = Math.max(rect.width, warp.pageRoot.clientWidth, 1)\n    const contentH = Math.max(\n        warp.pageRoot.scrollHeight,\n        warp.pageRoot.clientHeight,\n        rect.height,\n        1\n    )\n    const viewH =\n        typeof window !== \"undefined\"\n            ? window.innerHeight || rect.height\n            : rect.height\n    const yInRoot = Math.max(0, warp.pageRoot.scrollTop || -rect.top)\n\n    if (contentW !== warp.lastW || contentH !== warp.lastH) {\n        warp.filter.setAttribute(\"x\", \"0\")\n        warp.filter.setAttribute(\"y\", \"0\")\n        warp.filter.setAttribute(\"width\", String(contentW))\n        warp.filter.setAttribute(\"height\", String(contentH))\n        for (const node of [warp.edgeMask, warp.yMap, warp.xMap]) {\n            node.setAttribute(\"x\", \"0\")\n            node.setAttribute(\"y\", \"0\")\n            node.setAttribute(\"width\", String(contentW))\n            node.setAttribute(\"height\", String(contentH))\n        }\n        setHref(warp.yMap, buildYMap(contentW, contentH))\n        setHref(warp.xMap, buildXMap(contentW, contentH))\n        warp.lastW = contentW\n        warp.lastH = contentH\n        warp.lastMaskKey = \"\"\n    }\n\n    const edgeFrac = 0.14 + runtime.edgeDepth * 0.14\n    const maskKey = [\n        Math.round(yInRoot),\n        Math.round(viewH),\n        Math.round(contentW),\n        Math.round(contentH),\n        runtime.topEnabled ? 1 : 0,\n        runtime.bottomEnabled ? 1 : 0,\n        edgeFrac.toFixed(3),\n    ].join(\":\")\n\n    if (maskKey !== warp.lastMaskKey) {\n        setHref(\n            warp.edgeMask,\n            buildViewportEdgeMask({\n                width: contentW,\n                height: contentH,\n                yInRoot,\n                viewH,\n                edgeFrac,\n                topEnabled: runtime.topEnabled,\n                bottomEnabled: runtime.bottomEnabled,\n            })\n        )\n        warp.lastMaskKey = maskKey\n    }\n\n    if (reducedMotion) {\n        warp.dispY.setAttribute(\"scale\", \"0\")\n        warp.dispX.setAttribute(\"scale\", \"0\")\n        return\n    }\n\n    const depth = Math.max(0.2, runtime.edgeDepth)\n    const curve = Math.max(0.35, runtime.curvature)\n    const stretch = Math.max(0.25, runtime.stretch)\n    const i = Math.max(0.35, intensity)\n    // Typed band: ~14–40px class — far below the old ~73px disaster\n    const yScale = (14 + i * 26) * depth * curve\n    const xScale = (5 + i * 12) * depth * stretch\n    warp.dispY.setAttribute(\"scale\", String(yScale))\n    warp.dispX.setAttribute(\"scale\", String(xScale))\n\n    // Framer may wipe inline filters on reconcile — re-assert every frame\n    const expected = `url(#${warp.filterId})`\n    for (const el of warp.targets) {\n        if (el.style.filter !== expected) {\n            el.style.filter = expected\n            ;(el.style as CSSStyleDeclaration & { WebkitFilter?: string }).WebkitFilter =\n                expected\n        }\n    }\n}\n\nfunction usePrefersReducedMotion(): boolean {\n    const [reduced, setReduced] = React.useState(false)\n    React.useEffect(() => {\n        if (typeof window === \"undefined\") return\n        const mq = window.matchMedia(\"(prefers-reduced-motion: reduce)\")\n        const sync = () => setReduced(mq.matches)\n        sync()\n        mq.addEventListener(\"change\", sync)\n        return () => mq.removeEventListener(\"change\", sync)\n    }, [])\n    return reduced\n}\n\n/**\n * Edge refraction — Fixed fullscreen overlay above page content.\n * Warps real page siblings via SVG feDisplacementMap (no Slot, no glass WebGL rim).\n *\n * @framerSupportedLayoutWidth any-prefer-fixed\n * @framerSupportedLayoutHeight any-prefer-fixed\n */\nexport default function EdgeRefractionShader(props: EdgeRefractionShaderProps) {\n    const {\n        topEnabled = true,\n        bottomEnabled = true,\n        edgeDepth = 0.95,\n        curvature = 1.15,\n        stretch = 0.95,\n        scrollResponse = 1,\n        idleAmount = 0,\n        idleSpeed = 0.9,\n        style,\n    } = props\n\n    const isStatic = useIsStaticRenderer()\n    const reducedMotion = usePrefersReducedMotion()\n    const wrapRef = React.useRef<HTMLDivElement | null>(null)\n    const warpRef = React.useRef<ContentWarp | null>(null)\n    const rafRef = React.useRef(0)\n    const ensureRafRef = React.useRef<() => void>(() => {})\n    const isAnimatingRef = React.useRef(false)\n\n    const runtimeRef = React.useRef<RuntimeParams>({\n        topEnabled,\n        bottomEnabled,\n        edgeDepth,\n        curvature,\n        stretch,\n        scrollResponse,\n        idleAmount,\n        idleSpeed,\n    })\n    const motionRef = React.useRef({\n        smoothVel: 0,\n        targetVel: 0,\n        scrollKick: 0,\n        lastScrollY: 0,\n        lastScrollT: 0,\n        quietSince: 0,\n    })\n\n    const isAnimating = !reducedMotion && !isStatic\n\n    React.useEffect(() => {\n        isAnimatingRef.current = isAnimating\n        if (isAnimating) ensureRafRef.current()\n    }, [isAnimating])\n\n    React.useEffect(() => {\n        runtimeRef.current = {\n            topEnabled,\n            bottomEnabled,\n            edgeDepth,\n            curvature,\n            stretch,\n            scrollResponse,\n            idleAmount,\n            idleSpeed,\n        }\n        if (warpRef.current) warpRef.current.lastMaskKey = \"\"\n        ensureRafRef.current()\n    }, [\n        topEnabled,\n        bottomEnabled,\n        edgeDepth,\n        curvature,\n        stretch,\n        scrollResponse,\n        idleAmount,\n        idleSpeed,\n    ])\n\n    const tick = React.useCallback(\n        (t: number) => {\n            const warp = warpRef.current\n            const runtime = runtimeRef.current\n            const motion = motionRef.current\n            const shouldAnimate = isAnimatingRef.current\n\n            motion.smoothVel += (motion.targetVel - motion.smoothVel) * 0.12\n            motion.scrollKick +=\n                (Math.abs(motion.smoothVel) - motion.scrollKick) * 0.1\n            motion.targetVel *= 0.93\n\n            const idle = shouldAnimate\n                ? Math.sin(t * 0.001 * runtime.idleSpeed) * runtime.idleAmount\n                : 0\n            const scrollAmt = shouldAnimate\n                ? (Math.abs(motion.smoothVel) + motion.scrollKick) *\n                  runtime.scrollResponse\n                : 0\n            // Static canvas: readable warp preview. Live: scroll-driven floor.\n            const intensity = Math.min(\n                2,\n                isStatic\n                    ? 0.7\n                    : shouldAnimate\n                      ? 0.4 + scrollAmt + Math.abs(idle)\n                      : 0.35\n            )\n\n            if (warp) {\n                syncContentWarp(warp, runtime, intensity, reducedMotion)\n            }\n\n            if (!shouldAnimate) {\n                rafRef.current = 0\n                return\n            }\n\n            const busy =\n                scrollAmt > QUIET_INTENSITY ||\n                runtime.idleAmount > 0 ||\n                Math.abs(motion.targetVel) > 0.001 ||\n                motion.scrollKick > QUIET_INTENSITY\n            if (busy) {\n                motion.quietSince = 0\n                rafRef.current = requestAnimationFrame(tick)\n                return\n            }\n            if (!motion.quietSince) motion.quietSince = t\n            if (t - motion.quietSince < QUIET_MS) {\n                rafRef.current = requestAnimationFrame(tick)\n                return\n            }\n            rafRef.current = 0\n        },\n        [isStatic, reducedMotion]\n    )\n\n    const ensureRaf = React.useCallback(() => {\n        if (rafRef.current) return\n        motionRef.current.quietSince = 0\n        rafRef.current = requestAnimationFrame(tick)\n    }, [tick])\n\n    React.useEffect(() => {\n        ensureRafRef.current = ensureRaf\n    }, [ensureRaf])\n\n    // Mount SVG warp on real page content siblings\n    React.useEffect(() => {\n        if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n            return\n        }\n        if (reducedMotion) return\n\n        const wrap = wrapRef.current\n        if (!wrap) return\n\n        let cancelled = false\n        let tries = 0\n\n        const mount = () => {\n            if (cancelled) return\n            const host = wrap.parentElement\n            const page = resolvePageRoot(wrap)\n            if (!host || !page) {\n                if (tries++ < 30) window.setTimeout(mount, 40)\n                return\n            }\n            if (warpRef.current) {\n                destroyContentWarp(warpRef.current)\n                warpRef.current = null\n            }\n            const warp = createContentWarp(\n                page,\n                host,\n                topEnabled,\n                bottomEnabled\n            )\n            if (!warp) return\n            warpRef.current = warp\n            syncContentWarp(\n                warp,\n                runtimeRef.current,\n                isStatic ? 0.7 : 0.55,\n                false\n            )\n            ensureRaf()\n        }\n\n        mount()\n\n        return () => {\n            cancelled = true\n            if (warpRef.current) {\n                destroyContentWarp(warpRef.current)\n                warpRef.current = null\n            }\n        }\n    }, [bottomEnabled, ensureRaf, isStatic, reducedMotion, topEnabled])\n\n    React.useEffect(() => {\n        if (typeof window === \"undefined\") return\n        const motion = motionRef.current\n        motion.lastScrollY = window.scrollY\n        motion.lastScrollT =\n            typeof performance !== \"undefined\" ? performance.now() : 0\n\n        const onScroll = () => {\n            if (typeof performance === \"undefined\") return\n            const now = performance.now()\n            const y = window.scrollY\n            const dt = Math.max(8, now - motion.lastScrollT)\n            motion.targetVel = Math.max(\n                -5,\n                Math.min(5, (y - motion.lastScrollY) / dt)\n            )\n            motion.lastScrollY = y\n            motion.lastScrollT = now\n            motion.quietSince = 0\n            if (warpRef.current) warpRef.current.lastMaskKey = \"\"\n            ensureRafRef.current()\n        }\n\n        if (!reducedMotion && !isStatic) {\n            window.addEventListener(\"scroll\", onScroll, { passive: true })\n        }\n\n        // One sync for static canvas preview\n        ensureRaf()\n\n        return () => {\n            window.removeEventListener(\"scroll\", onScroll)\n            if (rafRef.current) {\n                cancelAnimationFrame(rafRef.current)\n                rafRef.current = 0\n            }\n        }\n    }, [ensureRaf, isStatic, reducedMotion, tick])\n\n    return (\n        <div\n            ref={wrapRef}\n            style={{\n                position: \"relative\",\n                width: \"100%\",\n                height: \"100%\",\n                overflow: \"visible\",\n                pointerEvents: \"none\",\n                ...style,\n            }}\n            aria-hidden={true}\n        />\n    )\n}\n\nEdgeRefractionShader.displayName = \"Edge Refraction Shader\"\n\naddPropertyControls(EdgeRefractionShader, {\n    topEnabled: {\n        type: ControlType.Boolean,\n        title: \"Top\",\n        defaultValue: true,\n    },\n    bottomEnabled: {\n        type: ControlType.Boolean,\n        title: \"Bottom\",\n        defaultValue: true,\n    },\n    edgeDepth: {\n        type: ControlType.Number,\n        title: \"Edge Depth\",\n        defaultValue: 0.95,\n        min: 0,\n        max: 1.5,\n        step: 0.01,\n    },\n    curvature: {\n        type: ControlType.Number,\n        title: \"Curvature\",\n        defaultValue: 1.15,\n        min: 0,\n        max: 2,\n        step: 0.01,\n    },\n    stretch: {\n        type: ControlType.Number,\n        title: \"Stretch\",\n        defaultValue: 0.95,\n        min: 0,\n        max: 2.2,\n        step: 0.01,\n    },\n    scrollResponse: {\n        type: ControlType.Number,\n        title: \"Scroll\",\n        defaultValue: 1,\n        min: 0,\n        max: 1.5,\n        step: 0.01,\n    },\n    idleAmount: {\n        type: ControlType.Number,\n        title: \"Idle Amt\",\n        defaultValue: 0,\n        min: 0,\n        max: 0.7,\n        step: 0.01,\n    },\n    idleSpeed: {\n        type: ControlType.Number,\n        title: \"Idle Speed\",\n        defaultValue: 0.9,\n        min: 0.1,\n        max: 3,\n        step: 0.01,\n    },\n})\n";
const f=(await framer.getCodeFiles()).find(x=>(x.name||"").includes("Edge_Refraction"));
if(!f) throw new Error("missing");
await f.setFileContent(content);
console.log(JSON.stringify({ok:true,lines:content.split("\n").length,hasWebGL:/getContext/.test(content),hasWarp:content.includes("createContentWarp"),hasMask:content.includes("buildViewportEdgeMask")}));
