import type { PluginEngine } from "./engine.ts";
export type Ownership = {
    type: "project";
} | {
    type: "external";
    name: string;
};
export interface VectorSetData {
    id: string;
    name: string;
    owner: Ownership;
}
export interface VectorSetItemData {
    id: string;
    name: string;
    insertUrl: string;
    iconUrl: string;
    moduleId: string;
}
export interface VectorSetItemVariable {
    type: "number" | "color";
    id: string;
    name: string;
}
/**
 * A set of vector icons available in the project.
 *
 * @alpha
 */
export declare class VectorSet {
    #private;
    id: string;
    name: string;
    owner: Ownership;
    constructor(data: VectorSetData, engine: PluginEngine);
    /**
     * Get all vector items in this set.
     *
     * @returns An array of VectorSetItem instances.
     */
    getItems(): Promise<VectorSetItem[]>;
}
/**
 * An individual vector icon within a VectorSet.
 *
 * @alpha
 */
export declare class VectorSetItem {
    #private;
    id: string;
    name: string;
    insertUrl: string;
    iconUrl: string;
    constructor(data: VectorSetItemData, engine: PluginEngine);
    /**
     * Get the customizable variables (e.g. color, stroke width) for this vector item.
     *
     * @returns An array of variable definitions.
     */
    getVariables(): Promise<VectorSetItemVariable[]>;
}
