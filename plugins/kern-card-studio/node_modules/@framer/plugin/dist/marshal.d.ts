import { $framerInternal } from "./internal.ts";
export declare const pluginMarshalTag = "plugin-marshal";
export type Marshaled<T> = T extends {
    [$framerInternal.marshal]: () => unknown;
} ? ReturnType<T[typeof $framerInternal.marshal]> : T extends string & {} ? T : T extends number & {} ? T : T extends object ? {
    [K in keyof T]: Marshaled<T[K]>;
} : T;
export declare function isSelfMarshalable<T>(value: T): value is T & {
    [$framerInternal.marshal]: () => Marshaled<T>;
};
export declare function marshal<T>(value: T): Marshaled<T>;
