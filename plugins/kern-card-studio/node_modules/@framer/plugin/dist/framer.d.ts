import { type PluginApiContext } from "./engine.ts";
import { FramerPluginAPI } from "./framerAPI.ts";
export declare const framer: FramerPluginAPI;
/**
 * @internal
 * Creates a fresh Framer API instance with custom context.
 * For edge runtime use only (Cloudflare Workers, etc) to avoid shared state across requests.
 */
export declare function createFramerInstance(context: PluginApiContext): FramerPluginAPI;
