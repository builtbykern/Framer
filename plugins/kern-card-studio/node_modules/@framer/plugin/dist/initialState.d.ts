import type { Mode } from "./messages.ts";
type PickModes<T extends Mode> = Extract<Mode, T>;
export type InitialState = {
    mode: Mode;
    intent: "plugin/open";
} | {
    mode: PickModes<"collection" | "syncManagedCollection" | "configureManagedCollection">;
    intent: "collection/add";
};
export {};
