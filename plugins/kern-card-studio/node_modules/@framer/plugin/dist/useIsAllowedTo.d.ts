import type { ProtectedMethod } from "./permissions.ts";
/**
 * React hook to check if user's permissions allow them to execute all of `methods`.
 * Re-renders when permissions change.
 *
 * @param methods - The methods to check, at least one.
 * @returns `true` if all of the given methods can be executed, `false` otherwise.
 *
 * @example
 * ```tsx
 * const isAllowedToRemoveItem = useIsAllowedTo("CollectionItem.remove")
 * <button disabled={!isAllowedToRemoveItem}>Remove Item</button>
 * ```
 */
export declare function useIsAllowedTo(...methods: [ProtectedMethod, ...ProtectedMethod[]]): boolean;
